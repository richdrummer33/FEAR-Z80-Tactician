name: Hero hierarchical sprite LOD lit-room ROM

on:
  push:
    branches:
      - temp/hero-nested-distance-codec-lab
      - temp/hero-lod-shape-objective
    paths:
      - ".github/workflows/hero-sprite-lod-room-proof.yml"
      - "src/main_hero_sprite_lod_room_gg.c"
      - "tools/build_hierarchical_sprite_lod_pack.py"
  workflow_dispatch:

jobs:
  rom-proof:
    runs-on: ubuntu-22.04
    timeout-minutes: 40
    env:
      ASSET: assets/gg-hero/FullDoomguyclassic-single-notex.glb
    steps:
      - uses: actions/checkout@v4

      - name: Install mesh tools
        run: |
          cd tools/glb_rmb
          npm install --no-audit --no-fund

      - name: Import Doomguy
        run: |
          set -euo pipefail
          mkdir -p build/hero-room/hero-host build/hero-room/room-host \
                   build/hero-room/generated-hero build/hero-room/generated-room \
                   tools/generated
          node tools/glb_rmb/convert.mjs "$ASSET" tools/generated/doomguy_mesh.inc \
            --name doomguy --height 19 --up z \
            --visual-tris 5200 --shadow-tris 200 --recess-radius 0.5 \
            > build/hero-room/import.json

      - name: Bake hero oracle corpus and runtime pack
        run: |
          set -euo pipefail
          cc -std=c99 -O2 -Wall -Wextra -Wpedantic -Werror \
            -DROOM_BUNDLE_DOOMGUY_GENERATED -Isrc -Itools \
            tools/polar_baked_composite.c tools/room_mesh_bake.c \
            tools/room_bundle_poc_gen.c -lm -o build/hero-room/hero-bake
          ROOM_BUNDLE_HERO_CORPUS=1 \
          HERO_CORPUS_ANGLES=32 \
          HERO_CORPUS_BANDS=24,28,32,36 \
          HERO_CORPUS_PREVIEW_EVERY=0 \
            build/hero-room/hero-bake build/hero-room/hero-host \
            | tee build/hero-room/hero-bake.txt

          python3 tools/build_hierarchical_sprite_lod_pack.py \
            build/hero-room/hero-host/doomguy_dense_corpus.dhc \
            build/hero-room/generated-hero \
            --angles 32 --core-patterns 16 --refinement-patterns 16 \
            --core-sprite-base 176 --refine-sprite-base 160 \
            | tee build/hero-room/hero-pack.txt
          grep -q '^HERO_SPRITE_LOD_PACK_PASS$' build/hero-room/hero-pack.txt

      - name: Bake lit room with top 32 patterns reserved
        run: |
          set -euo pipefail
          sed 's/static uint16_t g_hw_tile_limit=HW_TILES;/static uint16_t g_hw_tile_limit=416u;/' \
            tools/polar_baked_composite.c \
            > build/hero-room/polar_baked_composite_416.c
          cc -std=c99 -O2 -Wall -Wextra -Wpedantic -Werror -Isrc -Itools \
            build/hero-room/polar_baked_composite_416.c \
            tools/room_mesh_bake.c tools/room_bundle_poc_gen.c \
            -lm -o build/hero-room/room-bake
          ROOM_BUNDLE_ONLY=0 \
            build/hero-room/room-bake build/hero-room/room-host \
            | tee build/hero-room/room-bake.txt
          python3 tools/room_bundle_pack_to_c.py \
            build/hero-room/room-host/room_bundle_poc.pack \
            build/hero-room/room-host/room_bundle_poc_canonical.bin \
            build/hero-room/generated-room \
            | tee build/hero-room/room-cgen.txt

      - name: Install GBDK 4.5.0
        run: |
          set -euo pipefail
          mkdir -p .toolchain
          curl -L --fail --retry 3 -o .toolchain/gbdk-linux64.tar.gz \
            https://github.com/gbdk-2020/gbdk-2020/releases/download/4.5.0/gbdk-linux64.tar.gz
          echo 'd7857a5f6d135ee4c249043ca26aad9f2ec8ab5d4106d97720d404114f42605c  .toolchain/gbdk-linux64.tar.gz' | sha256sum -c -
          tar -xzf .toolchain/gbdk-linux64.tar.gz -C .toolchain

      - name: Build Game Gear proof ROM
        run: |
          set -euo pipefail
          LCC="$PWD/.toolchain/gbdk/bin/lcc"
          HGEN="$PWD/build/hero-room/generated-hero"
          RGEN="$PWD/build/hero-room/generated-room"
          FLAGS=(-mz80:gg -debug -autobank -Wb-ext=.rel -Wl-j -Wm-yo256 \
                 -Isrc -I"$HGEN" -I"$RGEN")
          OBJS=()

          "$LCC" "${FLAGS[@]}" -c -o build/hero-room/main.o \
            src/main_hero_sprite_lod_room_gg.c
          "$LCC" "${FLAGS[@]}" -c -o build/hero-room/ntstate.o \
            src/tilesector_polar_ntstate_gg.s
          "$LCC" "${FLAGS[@]}" -c -o build/hero-room/ntupload.o \
            src/tilesector_polar_ntupload_raw_gg.s
          "$LCC" "${FLAGS[@]}" -c -o build/hero-room/hero-pack.o \
            "$HGEN/hero_sprite_lod_pack.c"
          "$LCC" "${FLAGS[@]}" -c -o build/hero-room/room-dispatch.o \
            "$RGEN/room_bundle_poc_dispatch.c"
          OBJS+=(build/hero-room/main.o build/hero-room/ntstate.o \
                 build/hero-room/ntupload.o build/hero-room/hero-pack.o \
                 build/hero-room/room-dispatch.o)

          for src in "$RGEN"/room_bundle_poc_data_bank*.c; do
            base="$(basename "${src%.c}")"
            obj="build/hero-room/${base}.o"
            "$LCC" "${FLAGS[@]}" -c -o "$obj" "$src"
            OBJS+=("$obj")
          done

          "$LCC" "${FLAGS[@]}" -Wm-yS \
            -o build/hero-room/GG-Hero-Hierarchical-Sprite-LOD-Room.gg \
            "${OBJS[@]}"
          wc -c build/hero-room/GG-Hero-Hierarchical-Sprite-LOD-Room.gg \
            | tee build/hero-room/ROM-size.txt
          sha256sum build/hero-room/GG-Hero-Hierarchical-Sprite-LOD-Room.gg \
            | tee build/hero-room/SHA256.txt

          for sym in _main _set_sprite_4bpp_data _move_sprite; do
            tok="$(awk -v s="$sym" '$2==s{print $1; exit}' \
              build/hero-room/GG-Hero-Hierarchical-Sprite-LOD-Room.sym)"
            test -n "$tok"
            printf '%s %s\n' "$sym" "$tok"
          done | tee build/hero-room/SYMBOLS.txt

      - name: Run in Gearsystem and capture orbit
        run: |
          set -euo pipefail
          git clone --depth 1 --branch 3.9.16 \
            https://github.com/drhelius/Gearsystem.git .toolchain/Gearsystem
          tools/build_gearsystem_tools.sh "$PWD/.toolchain/Gearsystem"
          mkdir -p build/hero-room/captures
          ROM=build/hero-room/GG-Hero-Hierarchical-Sprite-LOD-Room.gg
          SYM=build/hero-room/GG-Hero-Hierarchical-Sprite-LOD-Room.sym
          CORE=.toolchain/Gearsystem/platforms/libretro/gearsystem_libretro.so
          STATUS="$(awk '$2=="_g_hero_lod_room_status"{split($1,a,":");print a[2];exit}' "$SYM")"
          test -n "$STATUS"
          LIBRETRO_CAPTURE_DIR=build/hero-room/captures \
          LIBRETRO_CAPTURE_EVERY=30 \
            ./build/libretro_runner "$CORE" "$ROM" 480 \
            build/hero-room/final.ppm "$STATUS" 8 \
            | tee build/hero-room/emulator.txt
          test "$(find build/hero-room/captures -name 'frame_*.ppm' | wc -l)" -ge 12
          grep -q 'RAM addr=' build/hero-room/emulator.txt

      - uses: actions/upload-artifact@v4
        with:
          name: hero-hierarchical-sprite-lod-room-proof
          path: |
            build/hero-room/GG-Hero-Hierarchical-Sprite-LOD-Room.gg
            build/hero-room/GG-Hero-Hierarchical-Sprite-LOD-Room.sym
            build/hero-room/GG-Hero-Hierarchical-Sprite-LOD-Room.map
            build/hero-room/hero-pack.txt
            build/hero-room/generated-hero/hero_sprite_lod_pack.json
            build/hero-room/room-bake.txt
            build/hero-room/ROM-size.txt
            build/hero-room/SHA256.txt
            build/hero-room/SYMBOLS.txt
            build/hero-room/emulator.txt
            build/hero-room/final.ppm
            build/hero-room/captures/**
          if-no-files-found: error
